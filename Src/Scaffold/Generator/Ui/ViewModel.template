using System;

namespace @(Model.WebNamespace).Models
{
    public class @(Model.Entity.Name)ViewModel
    {
		@foreach(var property in Model.Entity.BasicProperties) {
		@:public @(property.TypeName) @(property.Name) { get; set; }
		}
    }
}